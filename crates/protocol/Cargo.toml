[package]
name = "stream-sync-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[lib]
name = "stream_sync_protocol"
path = "src/lib.rs"

[lints]
workspace = true
